# GitHub Container Registry에서 이미지를 가져올 때 사용
# 사용법: docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d

# GitHub 저장소 정보 (예: username/repository-name)
GITHUB_REPOSITORY=your-username/zzic-api

# 데이터베이스 비밀번호 (보안상 실제 배포 시에는 안전한 비밀번호로 변경)
POSTGRES_PASSWORD=your-secure-password
